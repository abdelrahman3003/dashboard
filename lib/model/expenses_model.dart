class ExpensesModel {
  final String icon, title, subtitle, money;

  ExpensesModel(
      {required this.icon,
      required this.title,
      required this.subtitle,
      required this.money});
}
